# Implementation Plans

Generated by the improve skill on 2026-06-16. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Implement phase-1 article QA/tag/scoring pipeline | P1 | M | — | DONE |
| 002 | Add phase-1 QA ingest workflow from selected articles to weighted FastGPT admission | P1 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 is self-contained and should be executed before any integration into `main.py` upload flow. It only builds the phase-1 agent core and tests.
- Plan 002 depends on Plan 001 because it consumes `KnowledgeBaseQAAgent`, `QAReport`, rubric scoring, and redline checks to implement the requested phase-1 product path: 人工精选文章 → 自动整理 QA → 生成文章标签 → 打 QA 分数 → 分级入库.
- Existing default upload commands should remain unchanged until Plan 002's explicit `qa-ingest` command has tests and dry-run verification.

## Progress notes

- 2026-06-16: Plan 001 marked DONE. Verified `python3 -m pytest -q` passed with `40 passed in 0.33s` during advisor review.
- 2026-06-16: Plan 002 written for `gpt-5.4-mini` executor. It is intentionally implementation-ready but not executed by this advisor session.
- 2026-06-17: Plan 002 marked DONE. Removed a duplicate `cmd_qa_ingest` in `main.py`, added `tests/test_qa_ingest.py` (9 offline tests) and `tests/test_fastgpt_sync.py` (3 offline payload tests), documented `qa-ingest` in `README.md`, `README.en.md`, and `tests/README.md`. Verified `python3 -m pytest -q` → `52 passed`. The `--qa-ingest` opt-in flag on `download-and-clean` (Step 6) was intentionally left out of scope to avoid rewriting that command; the explicit `qa-ingest` command is the phase-1 deliverable.

## Quality check

- Current implementation scope matches phase-1 core: 人工精选文章 → 自动整理 QA → 生成文章标签 → 打 QA 分数 → 分级入库.
- Plan 002 extends the core into a safe explicit ingest workflow and weighted upload metadata while keeping failed/low-quality content from auto-uploading.
- No real LLM or FastGPT network calls are required by the planned tests.

## Findings considered and rejected

- Full upload-flow integration in Plan 001: rejected for phase 1 because the user requested the agent design and core implementation first, and because automatic upload behavior should not be changed until the QA report schema is stable.
- Making existing `upload-file` / `upload-folder` QA-gated by default in Plan 002: rejected because it would silently change existing CLI behavior. Plan 002 adds an explicit `qa-ingest` command instead.
- Multi-agent architecture: rejected because current product direction explicitly asks for a single-agent + prompt approach.
- Complex independent tag score: rejected for phase 1 because `qa_tagging_design.md` says tag quality should affect QA usability, not become a separate complex score yet.
- Real LLM/FastGPT integration tests: rejected because this repo's test suite is currently offline and deterministic; live smoke tests should remain manual.
